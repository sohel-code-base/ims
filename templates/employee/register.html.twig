{% extends 'base.html.twig' %}

{% block title %}Register | IE{% endblock %}
{% block stylesheet %}
    <link href="{{ asset('css/bootstrap-toggle.min.css') }}" rel="stylesheet">
    <link rel="stylesheet" href="{{ asset('/css/animate.min.css') }}">
    <link rel="stylesheet" href="{{ asset('css/blue.css') }}">
    <style>
        .checkbox label, .radio label {
            padding-left: 10px;
            margin-bottom: 15px;
            font-weight: 700;
        }
        .preview-user-photo img{
            width: 120px;
            height: 120px;
            margin: 0 auto;
            border: 1px solid #ccc;
            padding: 5px;
        }
        .preview-user-signature img{
            width: 100%;
            height: 50px;
            margin: 0 auto;
            border: 1px solid #ccc;
            padding: 5px;
        }
        .invalid-feedback{
            display: block;
            font-weight: normal;
            color: red;
        }
        .invalid-feedback .badge{
            display: none;
        }
    </style>
{% endblock %}

{% block contentTitle %}
    New Employee! <small>Register new employee!</small>
{% endblock %}
{% block body %}

    <!-- Main content -->
    <section class="content">
{#        {% for flashError in app.flashes('verify_email_error') %}
            <div class="alert alert-danger" role="alert">{{ flashError }}</div>
        {% endfor %}#}
        <div class="box box-primary">
            <div class="box-header with-border">
                {#            <h3 class="box-title">General Elements</h3>#}
            </div>
            <!-- /.box-header -->
            <div class="box-body">
                {{ form_start(registrationForm) }}

                <div class="row">
                    <div class="col-md-6">
                        <div class="form-group">
                            {{ form_label(registrationForm.fullName, 'Full Name') }}
                            {{ form_widget(registrationForm.fullName,{'attr':{'autocomplete':'off','placeholder':'Name'}}) }}

                            {{ form_label(registrationForm.designation, 'Designation') }}
                            {{ form_widget(registrationForm.designation,{'attr':{'autocomplete':'off','placeholder':'Designation'}}) }}

                            {{ form_label(registrationForm.phone, 'Phone') }}
                            {{ form_widget(registrationForm.phone,{'attr':{'autocomplete':'off','placeholder':'Phone Number'}}) }}

                            {{ form_label(registrationForm.address) }}
                            {{ form_widget(registrationForm.address,{'attr':{'autocomplete':'off','placeholder':'Address'}}) }}

                            {{ form_label(registrationForm.joiningDate) }}
                            {{ form_widget(registrationForm.joiningDate,{'id': 'datepicker', 'attr':{'autocomplete':'off','placeholder':'dd-mm-yyyy'}}) }}
                        </div>
                        <div class="form-group">
                            {{ form_label(registrationForm.roles, 'Access', {'label_attr': {'class': 'bg-gray', 'style': 'padding-left: 10px; margin-top:10px'}}) }}
                            {{ form_widget(registrationForm.roles, {'attr': {'class': 'checkbox'}}) }}
                        </div>
                    </div>
                    <div class="col-md-6">
                        <div class="form-group bg-gray-light" style="padding: 0px 15px">
                            {{ form_label(registrationForm.username, 'Username') }}
                            {{ form_widget(registrationForm.username,{'attr':{'autocomplete':'off','placeholder':'Username'}}) }}

                            {{ form_label(registrationForm.password, 'Password') }}
                            {{ form_widget(registrationForm.password,{'type': 'text','attr':{'autocomplete':'off','placeholder':'Password'}}) }}
                            {{ form_help(registrationForm.password) }}
                        </div>
                        <div class="row" style="padding-left: 14px; min-height: 250px">
                            <div class="col-md-6">
                                <div class="form-group margin-bottom-none">
                                    {{ form_label(registrationForm.photo) }}
                                    {{ form_widget(registrationForm.photo, {'id': 'uploadPhoto'}) }}
                                </div>
                                <div class="form-group preview-user-photo text-center">

                                </div>

                            </div>
                            <div class="col-md-6">
                                <div class="form-group margin-bottom-none">
                                    {{ form_label(registrationForm.signature) }}
                                    {{ form_widget(registrationForm.signature, {'id': 'uploadSignature'}) }}
                                </div>
                                <div class="form-group preview-user-signature">

                                </div>
                            </div>
                        </div>
                        <div class="form-group text-center">
                            <label for="">Job Status</label>
                            {{ form_widget(registrationForm.jobStatus) }}
                        </div>
                    </div>
                </div>
                <div class="row">
                    <div class="col-md-6 col-md-offset-3 text-center">
                        {{ form_widget(registrationForm.Save) }}
                    </div>
                </div>

                {{ form_end(registrationForm) }}
            </div>
        </div>
    </section>
{% endblock %}
{% block javascripts %}
    <script src="{{ asset('/js/sweetalert2.min.js') }}"></script>
    <script src="{{ asset('js/icheck.min.js') }}"></script>
    <script src="{{ asset('js/bootstrap-toggle.min.js') }}"></script>
    <script src="{{ asset('js/employee.js') }}"></script>

    <script>
        $(function () {
            $("#datepicker").datepicker({
                autoclose: true,
                todayHighlight: true,
                format: 'dd-mm-yyyy',
            });
        });

        $(function () {
            $('input').iCheck({
                checkboxClass: 'icheckbox_square-blue',
                radioClass: 'iradio_square-blue',
                increaseArea: '20%' /* optional */
            });
        });

        //iCheck for checkbox and radio inputs
        $('input[type="checkbox"].minimal, input[type="radio"].minimal').iCheck({
            checkboxClass: 'icheckbox_minimal-blue',
            radioClass   : 'iradio_minimal-blue'
        })
    </script>
{% endblock %}
